import { createTheme } from "@mui/material/styles";

// MUI-Theme mit Ubuntu als Standard-Schriftart
const theme = createTheme({
	typography: {
		fontFamily: "'Dimbo',Roboto, sans-serif",
	},
});

export default theme;
